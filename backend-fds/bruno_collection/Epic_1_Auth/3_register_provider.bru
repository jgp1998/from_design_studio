meta {
  name: 3_Register_Provider
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/registrations/provider
  body: json
  auth: none
}

body:json {
  {
    "email": "provider@maker.com",
    "password": "SecurePassword123",
    "rut": "77123456-9",
    "companyName": "Maker Farm 3D"
  }
}

tests {
  test("Should return 201 Created and assign PENDING_APPROVAL", function() {
    expect(res.getStatus()).to.equal(201);
  });
}