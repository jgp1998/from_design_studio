meta {
  name: 1_Register_B2B
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/registrations/b2b
  body: json
  auth: none
}

body:json {
  {
    "email": "user@corporate.com",
    "password": "SecurePassword123",
    "rut": "76123456-0",
    "companyName": "ACME Corp"
  }
}

tests {
  test("Should return 201 Created and assign ROLE_CLIENT", function() {
    expect(res.getStatus()).to.equal(201);
  });
}