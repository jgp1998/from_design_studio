meta {
  name: 3_Webhook_Storage
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/webhooks/storage-files
  body: json
  auth: none
}

body:json {
  {
    "EventName": "s3:ObjectCreated:Put",
    "Key": "orders/client/uuid.stl"
  }
}

tests {
  test("Should process Storage Webhook", function() {
    expect(res.getStatus()).to.equal(200);
  });
}