meta {
  name: 4_Accept_Bid
  type: http
  seq: 1
}

patch {
  url: {{baseUrl}}/api/v1/orders/:orderId/bids/:bidId/status
  body: json
  auth: none
}

body:json {
  {
    "accept": true
  }
}

tests {
  test("Should accept one bid and reject others", function() {
    expect(res.getStatus()).to.equal(200);
  });
}