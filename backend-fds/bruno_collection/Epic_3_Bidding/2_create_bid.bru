meta {
  name: 2_Create_Bid
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/orders/:orderId/bids
  body: json
  auth: none
}

body:json {
  {
    "price_clp": 15000,
    "estimated_days": 3
  }
}

tests {
  test("Should create PENDING Bid", function() {
    expect(res.getStatus()).to.equal(201);
  });
}