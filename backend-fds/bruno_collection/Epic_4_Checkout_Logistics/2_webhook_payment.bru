meta {
  name: 2_Webhook_Payment
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/webhooks/payments/mercadopago
  body: json
  auth: none
}

body:json {
  {
    "action": "payment.created",
    "data": {"id": "12345"}
  }
}

tests {
  test("Should set Order to PAID_AND_IN_PRODUCTION", function() {
    expect(res.getStatus()).to.equal(200);
  });
}